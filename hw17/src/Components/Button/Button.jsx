import styled from '@emotion/styled'

const firstStyleButton = styled('button')(({thema}) => {
    
})

const Button = ({children}) => { 

    return ( 
        <>
        <button>   {children}</button>
        </>
    )
}